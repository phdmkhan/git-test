# 📄 제품 요구사항 정의서 (PRD)
**프로젝트명:** 분리형 로컬 데이터 분석 웹 애플리케이션 (Local Multi-Page Data Analysis Web App)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 설명
본 프로젝트는 데이터 분석 및 UI 개발 역량을 결합하여 로컬 환경에서 구동하는 **Flask 기반 다중 페이지(Multi-page) 웹 애플리케이션**을 구축하는 것을 목표로 합니다. 서로 다른 도메인과 방법론을 가진 두 가지 데이터 분석 모듈(보스턴 집값 회귀분석 및 전력 소비량 시계열 예측)을 단일 서버 내에서 독립적인 개별 웹페이지로 분리하여 구현함으로써, 각 분석 결과에 최적화된 개별 시각화 및 수치 리포트를 제공합니다.

### 1.2 프로젝트 핵심 가치
* **완전한 모듈화 및 자율성**: 각 분석 모듈은 백엔드 로직뿐만 아니라 프론트엔드 화면(HTML)까지 완전히 분리되어 개발되므로, 분석가 간의 코드 충돌을 최소화하고 독립적인 고도화가 가능합니다.
* **라우팅 기반 웹 구조**: 사용자가 메인 홈 화면을 거쳐 원하는 분석 페이지로 개별 진입하는 멀티 페이지 구조를 채택하여, 웹 서비스의 확장성과 도메인 분리 체계를 명확히 합니다.
* **경량성 및 무결성**: 로컬 환경에서 가볍게 구동할 수 있도록 무거운 데이터베이스나 복잡한 프런트엔드 프레임워크 없이 Flask와 기본 분석 라이브러리만을 활용합니다.

### 1.3 대상 사용자
* 로컬 환경에서 각자의 데이터 분석 모델 연산 결과를 독립된 웹 화면으로 분리하여 프로토타이핑하고, 브랜치 기반 협업 과정을 검증하려는 데이터 분석가 및 웹 개발자.

---

## 2. 사용자 시나리오 및 구현 우선순위

### 2.1 사용자 엔드투엔드(End-to-End) 시나리오
사용자가 로컬 환경에서 개별 웹페이지를 탐색하고 분석 결과를 소비하는 흐름입니다.

1. **서비스 구동**: 사용자는 로컬 터미널에서 Flask 웹 서버 구동 명령어를 입력합니다.
2. **메인 홈 접속**: 웹 브라우저를 열어 내비게이션 허브 역할을 하는 메인 홈 주소(`http://127.0.0.1:5000/`)로 접속합니다.
3. **분석 A 페이지 이동**: 사용자가 홈 화면에서 '보스턴 집값 회귀분석' 버튼을 클릭하면 `/reg` 경로로 이동하며, 백엔드에서 OLS 연산이 트리거되어 분석가 A가 설계한 전용 화면이 렌더링됩니다.
4. **분석 B 페이지 이동**: 상단 내비게이션 바를 통해 '전력 소비량 예측' 메뉴를 클릭하면 `/tsa` 경로로 이동하며, Holt-Winters 연산이 수행되어 분석가 B가 설계한 전용 예측 화면이 렌더링됩니다.
5. **시스템 종료**: 브라우저 창을 닫고 터미널 프로세스를 종료하여 서비스를 마칩니다.

### 2.2 구현 우선순위 (개괄식 명세)
* **우선순위 P0 (최우선 필수 기능)**
  * **분석 A 독립 페이지 개발**: `/reg` 라우트 연동, 단순 선형 회귀 OLS 모델 엔진 및 `reg.html` 단독 템플릿 구현
  * **분석 B 독립 페이지 개발**: `/tsa` 라우트 연동, Holt-Winters 시계열 예측 엔진 및 `tsa.html` 단독 템플릿 구현
* **우선순위 P1 (보통 순위 연동 기능)**
  * **메인 랜딩 홈 화면 개발**: 각 분석 페이지로 이동할 수 있는 허브 화면(`/`, `index.html`) 및 공통 내비게이션 바(Navbar) 구축
  * **페이지별 개별 이미지 스트리밍**: 각 라우트 요청 시마다 Matplotlib 그래프를 인메모리(`base64`)로 생성하여 해당 페이지 템플릿에 동적 임베딩하는 모듈 구현
* **우선순위 P2 (낮은 순위 예외 처리 기능)**
  * **데이터 파일 예외 처리**: `/reg` 또는 `/tsa` 진입 시 원본 데이터 누락을 감지하여 각 페이지 내에 에러 알림 UI 출력
  * **수치 테이블 포맷팅**: 분석 B 페이지의 대용량 예측 수치 가독성 처리를 위한 전용 데이터 포맷터 적용

---

## 3. 프로젝트 핵심 기능목록

### 3.1 필수 기능 (Must-Have)
* **F-01. 허브 랜딩 페이지 구축 (공통 메인)**
    * 서비스의 첫 관문인 루트 URL(`/`) 구조를 정의하고, 두 개의 분석 메뉴로 직관적으로 진입할 수 있는 링크 카드와 글로벌 내비게이션 바를 제공하는 기능.
* **F-02. 보스턴 집값 선형 회귀분석 전용 페이지 (분석 A)**
    * `/reg` 주소로 맵핑되며, 독립변수 `RM`과 종속변수 `CMEDV` 간의 OLS 회귀 연산을 수행하고 분석 통계 지표를 단독 화면에 출력하는 기능.
* **F-03. 회귀분석 Scatter Plot 단독 렌더링 (분석 A)**
    * 분석가 A의 화면 규격에 맞춰 산점도 및 최적 회귀 오버레이 선그래프를 고해상도로 독립 플로팅하는 기능.
* **F-04. 전력 소비량 시계열 예측 전용 페이지 (분석 B)**
    * `/tsa` 주소로 맵핑되며, Holt-Winters 알고리즘(Additive 모드, 주기 12)을 실행하여 향후 1개년(12달) 미래 예측치를 단독 화면에 출력하는 기능.
* **F-05. 시계열 추이 및 예측 통합 선그래프 단독 렌더링 (분석 B)**
    * 분석가 B의 화면 가로 폭을 최대로 활용하여 과거 소비 추이와 미래 예측 점선을 하나의 와이드 그래프로 결합 플로팅하는 기능.
* **F-06. 라우트별 인메모리 이미지 버퍼링**
    * 각 페이지가 호출될 때 해당하는 그래프만 파이썬 메모리(`io.BytesIO`) 내에서 생성 및 `base64` 인코딩 후 각각의 HTML 템플릿으로 스트리밍하는 기능.

---

## 4. 기술스택 및 제약사항

### 4.1 개발 언어 및 프레임워크
* **Python (v3.8 이상)** / **Flask (v2.0 이상)**: 다중 라우팅 경로(`/`, `/reg`, `/tsa`) 제어 및 개별 HTML 템플릿 렌더링.

### 4.2 데이터 분석 라이브러리 명세
* **Pandas / Statsmodels / Matplotlib / Seaborn** 동일 유지 (각 분석 파일 내부에서 독립적으로 임포트하여 실행).

### 4.3 프런트엔드 UI 기술
* **HTML5 / CSS3 / Bootstrap 5 (CDN)**: 단일 대시보드용 2분할 레이아웃을 폐기하고, 다중 파일 구조(`index.html`, `reg.html`, `tsa.html`)로 분할 관리.

### 4.4 기술 제약사항 (Constraints)
* **HTML 파일의 물리적 분리**: `templates` 폴더 내에 반드시 `index.html`(홈), `reg.html`(분석A), `tsa.html`(분석B)이 별도의 파일로 존재해야 합니다.
* **공통 레이아웃 유지**: 페이지가 서로 분리되더라도 상단 내비게이션 바(Navbar)는 공통 컴포넌트로 포함되어 사용자가 언제든 다른 분석 페이지나 홈으로 자유롭게 이동할 수 있어야 합니다.

---

## 5. 화면기술정의서 (UI Design & 디자인 가이드)

### 5.1 페이지별 와이어프레임 구조 (Multi-Page Layout)

#### 5.1.1 [메인 홈 페이지 - `index.html` (`/`)]
* **글로벌 내비게이션 바**: 상단 고정형 (Home / 분석 A(회귀분석) / 분석 B(시계열분석))
* **메인 영역**: 대형 웰컴 배너(Jumbotron 형태) 및 중앙 2열 카드 배치
    * **왼쪽 카드**: "보스턴 집값 회귀분석 바로가기" (클릭 시 `/reg` 이동)
    * **오른쪽 카드**: "전력 소비량 시계열 예측 바로가기" (클릭 시 `/tsa` 이동)

#### 5.1.2 [분석 A 페이지 - `reg.html` (`/reg`)]
* **글로벌 내비게이션 바**: 동일 유지 (회귀분석 메뉴에 `.active` 하이라이트 활성화)
* **상단 영역**: OLS 모델 가설 검정 유의성 알림 컴포넌트 (부트스트랩 Alert)
* **중단 영역**: OLS 주요 회귀 지표 요약 리포트 테이블 (가로로 넓게 배치)
* **하단 영역**: 대형 회귀분석 시각화 패널 (가로폭 전체 또는 `.col-lg-8` 중앙 정렬 배치)

#### 5.1.3 [분석 B 페이지 - `tsa.html` (`/tsa`)]
* **글로벌 내비게이션 바**: 동일 유지 (시계열분석 메뉴에 `.active` 하이라이트 활성화)
* **상단 영역**: 향후 1개년 예측 추이 통합 선그래프 와이드 뷰 패널 (시각적 흐름 파악 우선 배치)
* **하단 영역**: 향후 12개월 상세 예측 수치 테이블 영역

---

## 6. 상세기능 명세서 (개괄식 명세)

### 6.1 백엔드-프론트엔드 라우트별 데이터 매핑 규격

* **`/` (홈 라우트 규격)**
  * 호출 스크립트: `app.py` -> `index.html` 랜더링
  * 전달 데이터: 없음 (정적 메뉴 및 가이드 구조)
* **`/reg` (분석 A 라우트 규격)**
  * 호출 스크립트: `reg_analysis.py` 로직 수행 -> `reg.html` 랜더링
  * 매핑 데이터 객체: 
    * 통계 수치 딕셔너리: `stats` (`r2`, `coef`, `p_value`) -> 상단 통계 카드 테이블에 주입
    * 시각화 스트링: `plot_b64` -> 산점도 회귀선 `<img>` 태그의 `src`에 직결 주입
* **`/tsa` (분석 B 라우트 규격)**
  * 호출 스크립트: `tsa_analysis.py` 로직 수행 -> `tsa.html` 랜더링
  * 매핑 데이터 객체:
    * 시각화 스트링: `plot_b64` -> 와이드형 시계열 예측 통합 선그래프 `<img>` 태그의 `src`에 직결 주입
    * 예측 리스트: `forecast_list` ([`month`, `pred`]) -> 하단 수치 템플릿 테이블 반복문 렌더링에 주입

### 6.2 프론트엔드 컴포넌트 및 페이지 제어 규칙

* **내비게이션 활성화(Active) 제어 규칙**
  * 각 HTML 페이지는 현재 자신의 위치에 해당하는 내비게이션 링크 엘리먼트에 부트스트랩 `.active` 클래스를 명시하여 인지성 확보
  * 사용자가 어느 페이지에 있든 상단 바의 다른 분석 메뉴를 누르면 지연 없이 즉시 해당 라우트로 하이퍼링크 점프 수행
* **분석 B 페이지 테이블 스크롤 제거 규칙**
  * 이전 대시보드와 달리 단독 독립 페이지(`tsa.html`)를 사용하므로 세로 공간 제약이 완화됨
  * 기존 테이블의 `max-height` 제한 및 스크롤바 속성을 제거하고, 12개월치 데이터 행 전체(12줄)를 페이지 내에 온전하게 테이블 본문으로 펼쳐서 시각화 가시성 극대화
* **가설 검정 Alert 분리 규칙**
  * 가설 유의성 체크 알림창은 오직 `reg.html` 내에서만 작동하며, 다른 페이지의 UI 구조에 어떠한 레이아웃 왜곡이나 간섭도 주지 않도록 격리 제어

---

## 7. 프로젝트 테스팅 (데이터 및 모델 백테스팅 방법)

### 7.1 분석 A: 통계적 가설 검정 (Statistical Testing)
* **p-value 자동 검증**: 회귀 계수의 산출 결과물 중 p-value가 통계적 유의 수준인 `0.05`보다 작은지 체크합니다.
* **설명력 평가지표 검증**: 단순 선형 회귀의 데이터셋 설명력을 파악하기 위해 모델의 결정계수($R^2$)가 사전에 협의된 기준선인 `0.40` 이상을 기록하는지 유효성을 검증하고 이를 화면에 투명하게 리포팅합니다.

### 7.2 분석 B: 시계열 홀드아웃 백테스팅 (Hold-out Validation)
* **데이터 분할 알고리즘**: 전체 데이터셋의 시계열 인덱스 중 **가장 최근의 마지막 12개월(1년)** 데이터를 백테스팅을 위한 '테스트셋'으로 사전 격리하고, 이를 제외한 이전의 모든 데이터만을 활용하여 Holt-Winters 알고리즘을 학습시킵니다.
* **예측 신뢰성 측정 (Error Metrics)**:
    1. **MAPE (Mean Absolute Percentage Error)**: 격리된 실제 12달 데이터 수치와 모델이 예측한 12달 예측치 간의 오차율을 계산합니다. ($MAPE < 10\%$ 이하 시 모델 성능 '우수' 판정)
    2. **RMSE (Root Mean Squared Error)**: 예측값과 실제값 오차의 제곱평균제곱근을 구하여 오차의 절대적인 수치적 볼륨을 파악하고 백엔드 로그 또는 대시보드 검증 지표로 제공합니다.